/*
 * @Author WangYubo
 * @Date 11/26/2020
 * @Description text类型解析库
 */

#include <regex>

#include "libtext.hpp"

using namespace std;

KeyValueMap parserText::parse(KeyValueMap &rules, string &input) {
    KeyValueMap result;
    for (auto it = rules.begin(); it != rules.end(); ++it)
    {

    }
    return result;
}
